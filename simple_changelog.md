[2026-08-10 19:03:53 UTC] [build/Fix] make install-agent waits for the old service to leave the launchd domain before bootstrapping. bootout returns before teardown finishes, so the bootstrap that followed it raced and failed with "Bootstrap failed: 5: Input/output error" on a reinstall over a running agent.
[2026-08-10 18:50:35 UTC] [SpaceBadge/Fix] The daemon runs [NSApp run] instead of a bare NSRunLoop, so AppKit actually refreshes NSScreen. Under the old loop every screen value stayed frozen at whatever the displays looked like at login, which is mid-settle after a reboot, so the Mission Control strip was positioned against a screen size that no longer existed and sat clipped in the top-left corner. Root cause of both earlier strip-placement fixes failing to hold.
[2026-08-05 19:02:07 UTC] [spacetool/Fix] Bare-number queries resolve as ordinals before name matching. sw 1 was substring-matching a space named "messaging1" instead of going to Desktop 1; same resolver serves send, rm, and the MC digit tap.
[2026-08-05 18:30:54 UTC] [spacetool/Feature] New verbs: create [name] adds a desktop, rm <space> removes one, layout save/restore snapshots and rebuilds the whole named-desktop layout. All driven through Mission Control's accessibility tree (mc.spaces.add button, AXRemoveDesktop action on thumbnails) so the Dock stays coherent. Needs Accessibility.
[2026-08-05 17:49:09 UTC] [SpaceBadge/Fix] The Mission Control strip repairs its position again after a display change. The reorder fix's unchanged-text early return skipped the place: repair, so a monitor config switch left the strip displaced at CG 0,0 behind the MC chrome until the text next changed.
[2026-07-30 21:36:30 UTC] [SpaceBadge/Fix] Mission Control strip now survives a space reorder. It only refreshed when MC opened, so reordering inside MC left it stale, and its label was never resized when the text changed without changing width, which clipped the last name.
[2026-07-30 21:12:12 UTC] [spacetool/Feature] New `send <space>` verb: pushes the focused window to another space and leaves you where you are, the inverse of `bring`. Shim at ~/.local/bin/send. Also added make install-agent (writes the LaunchAgent and reloads the daemon) and make uninstall.
[2026-07-30 19:00:08 UTC] [SpaceBadge/Feature] Press 1-9 inside Mission Control to switch spaces. Reinstated after the 2026-07-20 revert, now that the switch goes through the Dock instead of the bridge. MC swallows the swipe gesture, so the daemon dismisses MC and waits for it to actually close before switching. Event tap is armed only while MC is up; needs Accessibility.
[2026-07-30 18:46:50 UTC] [spacetool/Fix] sw switches via the Dock's own swipe gesture instead of the SkyLight bridge. The bridged switch only moved the window server; the Dock kept its own index, so Mission Control drew stale previews and ctrl-arrow counted from the wrong desktop. Now verified before returning, and ~2x faster.
[2026-07-30 18:46:50 UTC] [SpaceBadge/Fix] Badges and the Mission Control strip survive a display change. A dock/undock moves our windows behind AppKit's back and NSWindow.frame goes stale, so position is now decided against the window server's real bounds and re-applied with a nudge; also resyncs on NSApplicationDidChangeScreenParametersNotification and re-asserts each badge's space.
[2026-07-20 17:40:13 UTC] [SpaceBadge/Revert] Pulled the 1-9 switch-from-Mission-Control feature. Switching spaces while MC is open corrupts WindowServer (stuck spaces bar, interleaved menu bars, cross-space window composite). Daemon is back to naming only and no longer needs Accessibility. Stable codesigning from codesign.env kept.
[2026-07-20 17:01:01 UTC] [SpaceBadge/Fix] Badge frame recomputes on every sync, so a resolution or Dock change repositions it (previously only a rename did); badge anchors to its own space's display instead of always the main screen.
[2026-07-19 18:57:39 UTC] [spacetools/Initial] spacename/sw/bring CLIs, SpaceBadge daemon (corner badge + Mission Control name strip), Raycast script commands, LaunchAgent
